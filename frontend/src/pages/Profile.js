import React from 'react';
import Navbar from '../components/Navbar';

const Profile = () => {
    return (
        <div>
            <Navbar />
            Voici le profil
        </div>
    );
};

export default Profile;